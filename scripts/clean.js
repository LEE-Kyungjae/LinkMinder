#!/usr/bin/env node
/**
 * Minimal clean script to remove files generated during manual experiments.
 * The project has no compiled output yet, so we simply document the intent.
 */

console.log("Nothing to clean. Remove generated files manually if needed.");
